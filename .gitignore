# The FiveM licence key lives here. Never commit it.
secrets.cfg

# Runtime state and recordings
resources/[local]/telemetry/data-*.jsonl
resources/[local]/infected/record.json
cache/

# Working copies left behind by live patching
*.bak
*.bak-*
*.bak[0-9]

# SBM web: public submissions and triage drafts (live on the server)
web/submissions/

# Written by chase/server/round.lua on the live server: whose turn it was.
resources/[local]/chase/.last-fugitive

# Written by nick-of-time/server/session.lua on the live server: the session
# seed, the scoreboard and whose turn it was.
resources/[local]/nick-of-time/.session
